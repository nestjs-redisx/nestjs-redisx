/**
 * Driver type identifier registered with the core driver registry.
 * Use it via `RedisModule.forRoot({ clients: { type: MEMORY_DRIVER_TYPE } })`.
 */
export const MEMORY_DRIVER_TYPE = 'memory';
